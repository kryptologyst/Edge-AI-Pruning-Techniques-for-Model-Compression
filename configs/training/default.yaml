"""Training configuration overrides."""

# Training configuration
training:
  optimizer: "adam"
  epochs: 10
  learning_rate: 0.001
  weight_decay: 1e-4
  scheduler: "cosine"
  early_stopping:
    patience: 5
    min_delta: 0.001
